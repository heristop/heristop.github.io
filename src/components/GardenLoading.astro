<div class="garden-loading" role="status" aria-live="polite" aria-label="Opening the garden">
  <div class="garden-loading__scene" aria-hidden="true">
    <span class="garden-loading__halo"></span>
    <img class="garden-loading__tile garden-loading__tile--back" src="/images/zazen/sol/moss-mid.png" alt="" width="128" height="128" />
    <img class="garden-loading__tile" src="/images/zazen/sol/stone-slab.png" alt="" width="128" height="128" />
    <img class="garden-loading__tile garden-loading__tile--front" src="/images/zazen/sol/moss-mid.png" alt="" width="128" height="128" />
    <img class="garden-loading__lantern" src="/images/zazen/decors/lantern-lit.png" alt="" width="64" height="128" />
    <span class="garden-loading__pilgrim"></span>
    <span class="garden-loading__spark"></span>
    <span class="garden-loading__spark garden-loading__spark--late"></span>
  </div>
  <p class="garden-loading__eyebrow">A small adventure awaits</p>
  <h1>Path of <em>Stones</em></h1>
  <div class="garden-loading__stones" aria-hidden="true">
    {Array.from({ length: 5 }, (_, index) => <span style={`--stone: ${index}`}></span>)}
  </div>
  <p class="garden-loading__message">Opening the garden<span aria-hidden="true">…</span></p>
  <p class="garden-loading__hint">Five ancient stones. One path home.</p>
  <a class="garden-loading__retry" href="/path-of-stones/">Taking a little longer? Try again ↗</a>
  <noscript><p>Enable JavaScript to enter the garden.</p></noscript>
</div>

<style>
  .garden-loading {
    min-height: calc(100svh - 64px);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 3rem 1.25rem 5rem;
    background: linear-gradient(#14232b88, #14232bf2), url("/images/zazen/sanctuary-dawn.webp") center / cover, #172731;
    color: #ebdfb9;
    text-align: center;
    font-family: Georgia, serif;
  }

  .garden-loading__scene {
    position: relative;
    width: 240px;
    height: 168px;
    margin-bottom: 1.8rem;
    isolation: isolate;
  }

  .garden-loading__tile {
    position: absolute;
    left: 56px;
    top: 65px;
    width: 128px;
    height: 128px;
    image-rendering: pixelated;
    border-radius: 0;
  }

  .garden-loading__tile--back { left: 88px; top: 49px; }
  .garden-loading__tile--front { left: 24px; top: 81px; }

  .garden-loading__lantern {
    position: absolute;
    left: 105px;
    top: 0;
    width: 64px;
    height: 128px;
    image-rendering: pixelated;
    filter: drop-shadow(0 0 12px #ffd07980);
    border-radius: 0;
  }

  .garden-loading__pilgrim {
    position: absolute;
    left: 65px;
    top: 48px;
    width: 48px;
    height: 80px;
    background: url('/images/zazen/persos/pilgrim.png') no-repeat 0 0 / 144px 320px;
    image-rendering: pixelated;
  }

  .garden-loading__halo {
    position: absolute;
    inset: -10px;
    z-index: -1;
    background: radial-gradient(ellipse, #d4bb6330, transparent 67%);
    animation: lantern-breathe 3s ease-in-out infinite alternate;
  }

  .garden-loading__spark {
    position: absolute;
    left: 42px;
    top: 45px;
    width: 3px;
    height: 3px;
    background: #e9ca83;
    box-shadow: 0 0 9px #e9ca8366;
    animation: firefly-rise 3s ease-in-out infinite;
  }

  .garden-loading__spark--late { left: 194px; top: 82px; animation-delay: -1.5s; }
  .garden-loading__eyebrow { color: #b9c5a1; font-size: 0.6rem; letter-spacing: 0.24em; text-transform: uppercase; }

  h1 {
    font-family: Georgia, serif;
    font-size: clamp(2.3rem, 6vw, 3.3rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    color: #f4e6bc;
    margin: 0.6rem 0 1.5rem;
  }

  h1 em { color: #d8ae6c; }
  .garden-loading__stones { display: flex; gap: 12px; height: 12px; margin-bottom: 1rem; }

  .garden-loading__stones span {
    width: 7px;
    height: 7px;
    background: #aab787;
    box-shadow: 0 2px 0 #081e1c;
    rotate: 45deg;
    animation: stone-glimmer 1.8s ease-in-out infinite;
    animation-delay: calc(var(--stone) * 160ms);
  }

  .garden-loading__message { margin: 0; color: #f0dbac; font-size: 0.95rem; }
  .garden-loading__hint { margin-top: 0.65rem; color: #acbda9; font-size: 0.8rem; font-style: italic; }

  .garden-loading__retry {
    margin-top: 2rem;
    color: #c8ccad;
    font-size: 0.72rem;
    text-underline-offset: 4px;
    visibility: hidden;
    animation: show-retry 0s 12s forwards;
  }

  .garden-loading__retry:focus-visible { outline: 2px solid #f4e6bc; outline-offset: 6px; }
  @keyframes show-retry { to { visibility: visible; } }
  @keyframes lantern-breathe { to { opacity: 0.45; } }
  @keyframes firefly-rise { 0%, 100% { opacity: 0; translate: 0 4px; } 50% { opacity: 1; translate: 0 -6px; } }
  @keyframes stone-glimmer { 0%, 80%, 100% { opacity: 0.35; translate: 0 0; } 40% { opacity: 1; translate: 0 -3px; background: #efd299; } }

  @media (prefers-reduced-motion: reduce) {
    .garden-loading__halo, .garden-loading__spark, .garden-loading__stones span { animation: none; }
    .garden-loading__retry { animation: none; visibility: visible; }
  }
</style>
